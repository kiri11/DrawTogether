<Page x:Class="DrawTogether.ViewModel.LevelsPageViewModel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ARP="clr-namespace:DrawTogether"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1280"
	Title="LevelsPageView">

    <Grid>
        <ListBox Name="levelsPage_listBox" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Padding" Value="0,0,30,0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <ARP:AnimatedWrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Content="Back" HorizontalAlignment="Left" Margin="50,0,0,50" VerticalAlignment="Bottom" Width="75" Name="levelsPage_backButton" Click="OnBackButtonClick"/>
    </Grid>
</Page>
